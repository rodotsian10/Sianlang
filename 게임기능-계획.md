# 게임 제작용 문법·데이터 기능 계획

상태: 계획. 아래 기능은 이번 0.3 변경에서 구현하지 않는다.

## 여러 게임 요소 관리

| 기능 | 용도 | 결정할 내용 |
| --- | --- | --- |
| 목록 | 적·탄환·아이템 저장 | 리터럴, 원소 타입, 인덱스 범위 규칙 |
| 추가·삭제·길이 | 생성/소멸과 개수 확인 | 함수/메서드 이름, 순회 중 삭제 규칙 |
| 목록 순회 | 모든 적 이동·그리기 | for 또는 전용 반복 문법 |
| 속성을 묶는 데이터 | 위치·체력·속도 묶기 | 간단한 레코드부터 시작할지 여부 |
| 속성 접근 | player.x, player.hp | 필드 추가/변경과 타입 규칙 |
| 모듈 | 플레이어·적·맵 소스 분리 | 파일 탐색, 공개 범위, import 문법 |

이번 `#가변인수` 구현에 필요한 불변 인수 묶음, `묶음[index]`, `len(묶음)`은 제공한다. 일반 목록 리터럴·추가/삭제·목록 순회는 별도 단계다. 함수/None 저장을 위한 `var`도 0.3에서 제공한다.

## 편의 문법·기본 함수

- 복합 대입: `+=`, `-=`, `*=`, `/=` 등
- 정수 몫 `//`, 거듭제곱 `**`
- `abs`, `min`, `max`, `round` 등
- 문자열 인덱싱·슬라이싱·검색·분리·치환
- 난수, 삼각함수, 제곱근, 시간 측정
- 파일 읽기·쓰기와 게임 저장 데이터

## 파일 전용 명령어

사용자 결정: 이번 `log`는 콘솔 출력만 구현하고, 파일은 추후 전용 명령어로 설계한다.

- 파일 열기/닫기, 읽기/쓰기, append
- 텍스트 인코딩, 저장 위치와 상대 경로 기준
- 실패 시 `try`/`catch`로 복구
- 저장 완료/flush와 자원 해제
- 게임 설정·진행 상태 저장 형식

현재 `log(..., file=None)`은 기본 콘솔 출력이다. 다른 `file=` 값은 지원하지 않으며 명시적 오류로 처리한다.

## 실시간 키 입력

사용자 아이디어: `key` 같은 전용 명령어로 키 값을 받는 방식 검토.

`input()`의 한 줄 입력 동작은 유지한다. 게임용 키 입력은 이번에 구현하지 않는다. 후속 설계에서는 눌림/누르는 중/뗌, 키 이름, 마우스 입력, 프레임마다 이벤트 처리 방식을 정한다.

## 게임 실행 기능

창 생성·종료 → 프레임 갱신·시간 차이 → 도형 그리기·키 입력 → 이미지·글자·소리 → 충돌 판정 순서로 검토한다.

첫 목표는 창 하나에서 도형 하나를 키보드로 움직이는 예제다. 이후 여러 적과 탄환을 다루면서 목록/데이터 묶음 문법을 확정한다.

## 이번에 먼저 구현한 관련 기능

- 콘솔 `log`의 여러 값, sep/end/flush와 `log.f` 문자열 포매팅
- 반복문의 else
- 함수 기본값, 이름 지정 인수, `#가변인수`
- 중첩 함수, 스코프 체인, 클로저, 함수 전달
- `None`, `try`/`catch`

위 항목의 현재 문법은 [문법 규칙서](설명서/시안랭-문법규칙서.md)를 따른다.
