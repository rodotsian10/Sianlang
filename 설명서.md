# SianLang 0.3

간단한 문법으로 2D 게임을 만드는 것을 목표로 하는 C 기반 언어입니다. 현재는 콘솔 기본 기능을 지원합니다.

## 다운로드해서 사용하기

첫 Windows x64 배포판은 **0.3.1**입니다. `dist/0.3.1/`의 ZIP 또는 VSIX를 전달하면 됩니다.
VSIX에 실행기가 포함되어 있어 사용자는 GCC, Python, Node.js를 설치하지 않아도 됩니다.
설치 방법은 [시작하기](배포/시작하기.md), 공개 배포 절차는 [배포 안내](배포/배포하기.md)를 참고하세요.

## 개발자용 빌드와 실행

```powershell
powershell -ExecutionPolicy Bypass -File .\build.ps1
.\Sianlang.exe code.sian
```

GCC, Python 3, Node.js가 필요합니다. 빌드 스크립트는 C 경고 검사, 실행기 회귀 테스트, 확장 테스트를 통과한 뒤 실행 파일을 갱신합니다. 간단히 빌드만 하려면 `gcc main.c -o Sianlang.exe`를 사용할 수 있습니다.

```sian
int count = 0
repeat 3
    log count
    count = count + 1
```

## 문서

- [문법 규칙서](설명서/시안랭-문법규칙서.md)
- [레퍼런스 목차](설명서/index.md)
- [개발 순서](개발순서.md)
- [VS Code 확장](sianlang-vscode/README.md)
- [수정 내역](CHANGELOG.md)
- [게임 기능 계획](게임기능-계획.md)
- [새 기능 실행 예제](features-demo.sian)

실행기 내부는 `src/common.h`, `lexer.h`, `parser.h`, `runtime.h`로 나뉩니다. `main.c`가 이 파일을 포함하므로 기존 한 파일 빌드 명령을 유지합니다. 공식 실행 파일은 `Sianlang.exe`이고 `SianlangA.exe`는 호환용 사본입니다.

0.3에는 log/log.f, 반복문의 else, 함수 기본값·이름 지정 인수·#가변인수, 클로저·함수 전달, None, try/catch가 추가되었습니다. 파일과 key 명령은 계획 단계입니다.
